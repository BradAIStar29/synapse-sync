name = "synapse-sync-api"
main = "index.ts"
compatibility_date = "2024-01-01"

[vars]
APP_URL = "https://bmai-synapse-sync.pages.dev"

# Secrets (set via: wrangler secret put STRIPE_SECRET_KEY)
# STRIPE_SECRET_KEY
# VITE_STRIPE_PUBLISHABLE_KEY
